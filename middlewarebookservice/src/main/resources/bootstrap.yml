spring:
  cloud:
    config:
      name: middleware-service
      discovery:
        enabled: true
        service_id: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8091/eureka/